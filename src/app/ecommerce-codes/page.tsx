
// This page was part of the feature being reverted.
// Returning null to prevent rendering errors if this route is accessed.
// E-commerce codes are handled by the HomeTabs component in normal mode.

export default function RemovedEcommerceCodesPage() {
  return null;
}
